package com.canvara.app.enums;

public enum ArtworkStatus {
    AVAILABLE,  // listed and open for purchase requests
    SOLD,       // already sold; no new requests accepted
    UNLISTED    // supplier has hidden it temporarily
}
